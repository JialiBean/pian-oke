import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.jialistudios.pianoke',
  appName: 'Pian-oke',
  webDir: 'dist',
  ios: {
    contentInset: 'never',
    backgroundColor: '#f8f7f3',
  },
}

export default config
